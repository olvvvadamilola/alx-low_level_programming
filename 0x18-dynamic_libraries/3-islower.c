#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * _islower - function to print lowercase
 * @c: lowercase
 *
 * Return: 0
 */
int _islower(int c)
{
	if (islower(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
